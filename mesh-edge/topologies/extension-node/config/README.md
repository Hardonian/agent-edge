# Extension node config assets

Keep minimal, device-local configuration artifacts here.

Examples that belong in this directory once implemented and verified:

- sensor calibration settings
- transport credentials or topic bindings
- wake/sleep timing knobs
- bounded retry parameters

Prefer small, explicit files that remain safe for OTA update workflows.
