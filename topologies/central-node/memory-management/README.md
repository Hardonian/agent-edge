# Central node memory-management assets

Reserve this directory for central-node modules or assets that handle heavier state and storage concerns, for example:

- aggregation state managers
- durable storage coordination
- retention or archival workflows
- explicit error journals tied to verified central-node behavior

This directory exists to keep large or stateful concerns separate from small operator config.
