# Central node config assets

Keep small static configuration files for central-node deployments here.

Examples that belong in this directory once implemented and verified:

- transport endpoint definitions
- MQTT topic maps
- service-level defaults
- environment-specific overrides that remain small and operator-auditable

Do not place large datasets, rolling logs, or persistent state snapshots here.
